<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lefur | Panadería Artesanal</title>
    <style>
        /* Diseño Base y Colores */
        :root {
            --primary: #C89F53;
            --bg-color: #FAECE3;
            --text-dark: #2C1E16;
            --whatsapp: #25D366;
        }
        body {
            font-family: 'Lato', sans-serif;
            margin: 0;
            padding: 0;
            color: var(--text-dark);
            background-color: var(--bg-color);
        }
        h1, h2, h3 {
            font-family: 'Playfair Display', serif;
        }
        
        /* Navegación */
        header {
            background-color: #fff;
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
        }
        nav a {
            margin-left: 20px;
            text-decoration: none;
            color: var(--text-dark);
            font-weight: bold;
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 100px 20px;
            background-color: #fff;
        }
        .hero h1 {
            font-size: 3em;
            margin-bottom: 10px;
        }
        .btn {
            display: inline-block;
            padding: 15px 30px;
            margin: 10px;
            background-color: var(--primary);
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }
        .btn-outline {
            background-color: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
        }

        /* Botón Flotante de WhatsApp (Paso 8.1) */
        .whatsapp-float {
            position: fixed;
            width: 60px;
            height: 60px;
            bottom: 40px;
            right: 40px;
            background-color: var(--whatsapp);
            color: #FFF;
            border-radius: 50px;
            text-align: center;
            font-size: 30px;
            box-shadow: 2px 2px 10px rgba(0,0,0,0.2);
            z-index: 100;
            display: flex;
            justify-content: center;
            align-items: center;
            text-decoration: none;
        }

        /* Responsive Móvil */
        @media (max-width: 768px) {
            header { padding: 20px; flex-direction: column; }
            nav a { margin: 0 10px; }
            .hero h1 { font-size: 2em; }
        }
    </style>
</head>
<body>

    <header>
        <h2>🥖 Lefur</h2>
        <nav>
            <a href="#inicio">Inicio</a>
            <a href="#nosotros">Nosotros</a>
            <a href="#menu">Menú</a>
            <a href="#contacto">Ubicación</a>
        </nav>
    </header>

    <section id="inicio" class="hero">
        <h1>El sabor de lo artesanal,<br>horneado cada mañana.</h1>
        <p>Panadería y repostería con ingredientes seleccionados y masa madre.</p>
        <a href="#contacto" class="btn">📍 Cómo llegar</a>
        <a href="https://wa.me/1234567890" target="_blank" class="btn btn-outline">📱 Pedir por WhatsApp</a>
    </section>

    <a href="https://wa.me/1234567890" class="whatsapp-float" target="_blank">
        💬
    </a>

</body>
</html>